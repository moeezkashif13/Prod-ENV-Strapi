{
  "kind": "collectionType",
  "collectionName": "orders",
  "info": {
    "singularName": "order",
    "pluralName": "orders",
    "displayName": "Order"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "PlacedOrders": {
      "type": "component",
      "repeatable": true,
      "component": "placed-orders.placed-orders"
    },
    "Status": {
      "type": "enumeration",
      "enum": [
        "Order Received",
        "Confirmed",
        "Dispatched",
        "Completed"
      ],
      "required": true
    },
    "Name": {
      "type": "string",
      "required": true
    },
    "Email": {
      "type": "email",
      "required": true
    }
  }
}
